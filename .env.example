APP_NAME="Asker"

BOT_NAME="Герман"
TOKEN_BOT_TOKEN=""

# Прокси для исходящих HTTP(S) запросов. Требуется на хостах, где api.telegram.org
# заблокирован (например, сервер в РФ). Если оставить пустыми — Go пойдёт напрямую.
HTTP_PROXY=""
HTTPS_PROXY=""
NO_PROXY="localhost,127.0.0.1"
